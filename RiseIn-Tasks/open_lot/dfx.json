{
  "canisters": {
    "open_lot_backend": {
      "candid": "src/open_lot_backend/open_lot_backend.did",
      "package": "open_lot_backend",
      "type": "rust"
    },
    "open_lot_frontend": {
      "dependencies": [
        "open_lot_backend"
      ],
      "source": [
        "src/open_lot_frontend/dist"
      ],
      "type": "assets",
      "build": [
        "bash -c 'BACKEND_ID=$(dfx canister id open_lot_backend) && cd src/open_lot_frontend && echo \"VITE_CANISTER_ID_OPEN_LOT_BACKEND=$BACKEND_ID\" > .env && echo \"VITE_DFX_NETWORK=local\" >> .env && npm install && npm run build'"
      ]
    }
  },
  "defaults": {
    "build": {
      "args": "",
      "packtool": ""
    }
  },
  "output_env_file": ".env",
  "version": 1
}